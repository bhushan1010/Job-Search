{
    "title": "Job Pipeline Deduplicator & Filter Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "datasetId": {
            "title": "Dataset ID",
            "type": "string",
            "description": "The Apify dataset ID from the scraper run to deduplicate.",
            "editor": "textfield"
        },
        "telegramBotToken": {
            "title": "Telegram Bot Token",
            "type": "string",
            "description": "Bot Token from @BotFather (e.g. 123456:ABC-DEF...).",
            "editor": "textfield",
            "isSecret": true
        },
        "telegramChatId": {
            "title": "Telegram Chat ID",
            "type": "string",
            "description": "Your numerical Telegram user or channel Chat ID.",
            "editor": "textfield"
        },
        "slackWebhookUrl": {
            "title": "Slack Incoming Webhook URL (Optional)",
            "type": "string",
            "description": "Optional Slack Incoming Webhook URL.",
            "editor": "textfield"
        },
        "sourceLabel": {
            "title": "Search Keyword / Source Label",
            "type": "string",
            "description": "Tag or keyword identifying the source run (e.g., 'ML Engineer - Pune').",
            "editor": "textfield",
            "default": "Job Pipeline Run"
        },
        "excludeTitleRegex": {
            "title": "Exclude Senior/Lead Roles Regex",
            "type": "string",
            "description": "Regex pattern to identify and filter out experienced roles (case-insensitive).",
            "editor": "textfield",
            "default": "\\b(sr\\.?|senior|lead|principal|staff|director|vp|vice president|head of|manager|chief|architect|expert|team lead|5\\+|6\\+|7\\+|8\\+|10\\+|5 to 10|5-10)\\b"
        },
        "storeName": {
            "title": "Key-Value Store Name",
            "type": "string",
            "description": "Named Key-Value Store used across all tasks to maintain persistent deduplication state.",
            "editor": "textfield",
            "default": "JOB_PIPELINE_SEEN_STORE"
        }
    }
}
